# Documentation Update Plan

## Goal
Update `README.md` and `.md` files in the `docs/` directory to reflect the latest changes, specifically the nested configuration structure and the Engine tab redesign.

## Tasks
- [x] Task 1: List all `.md` files in `docs/` and identify target files. → Verify: List is complete.
- [x] Task 2: Review `README.md` for outdated config examples or feature descriptions. → Verify: Identified all outdated sections.
- [x] Task 3: Review `docs/` files for outdated config examples or feature descriptions. → Verify: Identified all outdated sections.
- [x] Task 4: Update `README.md` with new nested config examples and Engine tab details. → Verify: File is updated and valid.
- [x] Task 5: Update target `docs/` files with new nested config examples. → Verify: Files are updated and valid.

## Done When
- [x] All `.md` files in scope reflect the current state of the application.
- [x] All config examples use the new nested structure or mention backward compatibility correctly.
